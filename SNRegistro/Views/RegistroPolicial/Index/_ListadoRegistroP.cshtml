
<div class="row">
    <div class="col-md-12">
        <h3>Listado Registro Policial</h3>
    </div>
</div>
<div ng-controller="listadoRegistroPCtrl as vm" ng-cloak>
    <div class="row">
        <div class="col-md-12">

            <ul class="nav nav-tabs" style="margin-top:25px;">
                <li role="presentation" class="{{vm.menu.listado.class}}">
                    <a href="#" ng-click="vm.listadoFn()">Listado</a>
                </li>
                <li role="presentation" class="{{vm.menu.busqueda.class}}">
                    <a href="#" ng-click="vm.busquedaFn()">Busqueda</a>
                </li>
            </ul>
            <div style="margin-top:25px;" ng-show="vm.menu.listado.mostrar">
                <div class="alert alert-danger">
                    LISTADO
                </div>
            </div>
            <div style="margin-top:25px;" ng-show="vm.menu.busqueda.mostrar">
                @*@Html.Partial("Index/_FormBusqueda")*@
                <div class="alert alert-danger">
                    BUSQUEDA
                </div>
            </div>
        </div>

    </div>
    <div class="row">
        <div class="col-md-12">
            <table class="table table-condensed table-bordered table-hover">
                <thead>
                    <tr>
                        <th colspan="2">Acciones</th>
                        <th>Nro. de Registro</th>
                        <th>Ciudadano</th>
                        <th>Policia</th>
                        <th>Comisaria</th>
                        <th>Proceso</th>
                        <th>Acción</th>
                        <th>Comentario</th>
                    </tr>
                </thead>
                <tbody>
                    <tr ng-repeat="o in vm.RegistrosPoliciales">
                        <td><button title="Actualizar" class="btn btn-warning btn-sm" ng-click="vm.actualizar(o)"> <i class="glyphicon glyphicon-edit" /></button></td>
                        <td><button title="Eliminar" class="btn btn-danger btn-sm" ng-click="vm.eliminar(o)"><i class=" glyphicon glyphicon-remove" /></button></td>
                        <td>{{o.RegistroPolicialID}}</td>
                        <td>{{o.Ciudadano.Nombre + " " + o.Ciudadano.Apellido }}</td>
                        <td>{{o.Policia.Nombre + " " + o.Policia.Apellido }}</td>
                        <td>{{o.Comisaria.Nombre}}</td>
                        <td>{{o.AccionesPoliciale.ProcesosPoliciale.NombrePP}}</td>
                        <td>{{o.AccionesPoliciale.NombreAP}}</td>
                        <td>{{o.Comentario}}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
